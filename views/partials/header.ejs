<nav class="header-container">
  <h1>CodeWars</h1>
  <ul class="header-menu">
    <li>
      <a href="/">Home</a>
    </li>
    <li>
      <a href="/logout">Logout</a>
    </li>
    <% if (user) { %>
      <div class="current-user">
        <% if (user.photo_url) { %>
          <img src="<%= user.photo_url%>" alt="userphoto" width="40" height="40">
        <% } %>
        <p><%= user.username %></p>
      </div>
    <% } %>
  </ul>
</nav>
