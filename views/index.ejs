<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include("./partials/header.ejs") %>
    <div>
      <h1><%= title %></h1>
      <% if (problems.length) {%>
        <div class="problem-wrapper">
          <% for (let problem of problems) {%>
            <div class="problem-container">
              <h1><a href="/problems/<%= problem.problemId %>"><%= problem.title %></a></h1>
              <h3>Level: <%= problem.level %></h2>
              <h3>Completed Users: <%= problem.completedUsers.length %></h3>
            </div>
        </div>
        <%}%>
      <%}%>
    </div>
    <%- include("./partials/footer.ejs") %>
  </body>
</html>
