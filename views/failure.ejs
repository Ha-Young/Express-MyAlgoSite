<!DOCTYPE html>
<html>
  <head>
    <title>Codewars</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <nav>
      <ul>
        <li><img src="https://www.softlab.ntua.gr/~nickie/images/logo/codewars.png" width="150px" height="150px"></li>
          <li><a href="/logout">LOG OUT</a></li>
          <li>PROBLEM</li>
      </ul>
    </nav>
    <main>
      <% if (err) { %>
        <pre>에러입니다.<%= err.stack %> </pre>
      <% } else { %> 
        <ul>
          <% for (let i = 0; i < wrongResults.length; i++) { %>
            <% if (array) { %>
              <li>정답은 배열안에 <%= solution[i] %> 가 있어야 하지만 <%= wrongResults[i] %>가 있는걸로 나왔습니다.</li>
            <% } else { %> 
              <li>정답은 <%= solution[i] %> 이지만 <%= wrongResults[i] %>가 나왔습니다.</li>
            <% } %>
          <% } %>
          <li>분발해주세요!!!!!!!!!!!!!!!!!</li>
        </ul>
      <% } %>
    </main>
  </body>
</html>