<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>Failure</title>
</head>

<body>
  <div class="header">
    <img class='side-menu-img' onmouseenter="openNav()" src="/images/codewars-sidebar.png" alt="codewars-sidebar">
    <ul class="horizontal-list">
      <% if (user) { %>
      <li><a href="/auth/logout">Logout</a></li>
      <% } else { %>
      <li><a href="/auth/login">Login</a></li>
      <% } %>
      <li><a href="/">Homepage</a></li>
      <li><img class='nav-thumb' src="<%= user.thumbnail %>" alt="user_thumbnail"></li>
      <li><a href="/profile"><%= user.username %></a></li>
    </ul>
  </div>

  <div id="mySidenav" class="sidenav" onmouseleave="closeNav()">
    <a href="/">Home</a>
    <a href="/"><i class="fab fa-kickstarter-k"></i> Kata</a>
    <a href="/profile">Profile</a>
  </div>

  <script>
    function openNav() {
      document.getElementById("mySidenav").style.width = "250px";
    }

    /* Set the width of the side navigation to 0 */
    function closeNav() {
      document.getElementById("mySidenav").style.width = "0";
    }
  </script>
  <script src="https://kit.fontawesome.com/3f39b7ae50.js" crossorigin="anonymous"></script>

  <header>
    <h1>이런, 다시 시도해보시길 바랍니다!</h1>
  </header>
  
  <main>
    <pre>
      <% if (error !== undefined) { %>
      <%= error.stack %>
      <% } else if (failedTests) {  %>
        <ul>
          <% for (let i = 0; i < passedTests.length; i++) { %>
          <li class='passedTest'>Test Passed: Value === <%= passedTests[i].solution %></li>
          <% } %>
          <% for (let i = 0; i < failedTests.length; i++) { %>
          <li class='failedTest'>Expected: <%= failedTests[i].solution %>, instead got : <%= failedTests[i].submitted %></li>
          <% } %>
        </ul>
      <% } %> 
    </pre>
  </main>
</body>

</html>